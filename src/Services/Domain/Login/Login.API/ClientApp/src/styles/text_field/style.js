import { green } from '@material-ui/core/colors'
import { createMuiTheme } from '@material-ui/core/styles';

const textFieldsTheme = createMuiTheme({
    palette: {
        primary: green
    }
})

export default textFieldsTheme